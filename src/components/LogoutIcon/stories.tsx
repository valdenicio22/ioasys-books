import { Story, Meta } from '@storybook/react'
import LogoutIcon from '.'

export default {
  title: 'LogoutIcon',
  component: LogoutIcon
} as Meta

export const Default: Story = (args) => <LogoutIcon {...args} />
